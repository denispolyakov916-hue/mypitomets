# Django и Django REST Framework
# Обновлено в рамках рефакторинга (Этап 1.1)
# Django 5.1.5 - LTS версия с поддержкой до апреля 2026
Django==5.1.5
# DRF 3.15.2 - совместим с Django 5.1
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1

# CORS headers для коммуникации с фронтендом
# Обновлено для улучшения безопасности
django-cors-headers==4.6.0

# PostgreSQL адаптер
psycopg2-binary==2.9.10

# Обработка изображений
# Pillow 12.0.0 совместим с Django 5.1
Pillow==12.0.0

# Переменные окружения
python-dotenv==1.0.1

# Сервер для продакшена
# Обновлено для улучшения производительности
gunicorn==23.0.0
locust>=2.20.0

# Argon2id для хэширования паролей (рекомендуемый алгоритм OWASP)
argon2-cffi==23.1.0

# UUIDv7 для генерации уникальных идентификаторов
uuid7==0.1.0

# Транслитерация для генерации slug из кириллицы
unidecode==1.3.8

# Генерация iCalendar (.ics) для мероприятий («Новости и Мероприятия» + Apple/Google sync)
icalendar==5.0.13

# S3-совместимое облачное хранилище (Yandex Cloud Object Storage)
django-storages[s3]==1.14.4

# Тестирование: фабрики для генерации тестовых данных
factory-boy==3.3.1

# JSON логирование (опционально, для структурированных логов)
# pythonjsonlogger==2.0.7
