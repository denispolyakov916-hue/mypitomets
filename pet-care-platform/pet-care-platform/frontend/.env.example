# =============================================================================
# Пример переменных окружения фронтенда (Vite)
# Скопируйте в frontend/.env. Переменные читаются Vite (vite.config.js).
# =============================================================================

# Хост и порт бэкенда для прокси /api в режиме разработки
VITE_BACKEND_HOST=localhost
VITE_BACKEND_PORT=8077
